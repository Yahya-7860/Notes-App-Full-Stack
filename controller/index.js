const {
  handleAddNote,
  handleGetNotes,
  handleDeleteNote,
  handleUpdateNote,
} = require("./notesController");

module.exports = {
  handleAddNote,
  handleGetNotes,
  handleDeleteNote,
  handleUpdateNote,
};
